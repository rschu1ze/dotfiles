[user]
    name = Robert Schulze
    email = robert@clickhouse.com
    signingkey = 5BF99C54A67FA475E23625EF26703B55FB13728A
[commit]
    gpgsign = true
[core]
    editor = nvim
    pager = less -i # search case-insensitively
[credential]
    helper = store
[submodule]
    recurse = true # make 'git pull' and other commands work with submodules
[alias]
    a = add
    b = branch
    bd = branch --delete --force
    bm = branch --move --force
    c = commit
    ca = commit --amend
    co = alias_no_longer_needed # deliberately gives an error ... alias can be removed once enough muscle memory is there
    cob = use_git_sw_instead # deliberately gives an error ... alias can be removed once enough muscle memory is there
    cp = cherry-pick
    dc = diff --cached
    f = fetch --prune
    g = grep
    l = log --pretty=format:'%C(red)%h %C(yellow)%ad %Cblue%an%Cgreen%d %Creset%s' --date=iso
    m = merge
    pu = push
    puf = push --force-with-lease # safer than --force
    pl = pull --prune --rebase
    r = rebase
    st = status --short --branch --ignore-submodules # too slow otherwise
    sh = show
    sw = switch
    swc = switch --create
    fuckit = !git reset --hard && git clean --force -d
    # Worktrees don't play well with submodules
    # wt = worktree
    # wta = worktree add -b
    unstage = reset HEAD
